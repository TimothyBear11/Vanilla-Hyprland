name: flatpak-setup-installs
type: shell
commands:
  # Install Flatpak and Discover with the Flatpak backend
  - apt-get update
  - >-
    apt-get install -y --no-install-recommends
    flatpak
    plasma-discover
    plasma-discover-backend-flatpak

 
